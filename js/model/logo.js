/* Stores information about a logo/link. */
export class Logo {
  constructor(name, link, logoUrl, altText) {
    this.name = name;
    this.link = link;
    this.logoUrl = logoUrl;
    this.altText = altText;
  }
}
